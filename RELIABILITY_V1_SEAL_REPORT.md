# RELIABILITY_V1_SEAL_REPORT

**日期**：2026-08-21
**阶段**：Reliability v1 Final Seal（post-merge，生产实际环境验证）
**仓库**：ZTKyo/deepseek-harness-desktop（`_release-staging`）

---

## 1. main SHA

```
3f4208c7c01b9bdf437de421809c098f6d778ee4
```

- `git fetch origin` 后 `origin/main` = `3f4208c7c01b9bdf437de421809c098f6d778ee4` ✅
- 本地 `main` = 同一 SHA，工作区干净，`main...origin/main` 无 ahead/behind ✅
- 该 SHA 即 PR #2 "Reliability v1: Last Good Authority + Transaction 2.0 + True Safe Mode + CI (#2)" 合并提交 ✅

## 2. Post-Merge Final Health（真实生产环境，端口 3080）

| 检查项 | 结果 | 证据 |
|---|---|---|
| DSH 版本 | **PASS** | `dsh --version` → 0.1.0-rc.8 |
| Process Identity | **PASS** | PID 2972, node.exe, 命令行含 dsh web 特征，5 项身份检查全过 |
| host.describe | **PASS** | Test-DshApiReady → api_ready（host.describe RPC ok） |
| session.list | **PASS** | RPC ok，返回 264 个 session items |
| events.mux | **PASS** | WebSocket `ws://127.0.0.1:3080/api/events.mux` open |
| events.host | **PASS** | WebSocket `ws://127.0.0.1:3080/api/events.host` open |
| Renderer | **PASS** | GET `/` → 200 且 body 含 `__DSH_BOOT__` |
| client_ready | **PASS** | Test-DshReadiness -RequireWebSockets → client_ready |
| COMMIT_READY | **PASS** | Test-CommitReadiness 7 项全绿 + stable window 10s |
| Guardian running | **PASS** | powershell PID 9608 运行 `dsh-guardian.ps1 -Port 3080`；Scheduled Task "DSH Guardian" = Running |
| Restart Budget | **PASS** | hourAttempts=2（正常窗口内），lastSuccess 12:28:50，无 circuit/pause |
| Sessions readable | **PASS** | 264 items 可读；`~/.dsh/sessions` 目录可访问 |
| Diagnostics secret redaction | **PASS** | dsh-diagnostics.ps1 → "Secret redaction: PASS"；13 个输出文件独立扫描零泄漏 |
| Tool Output Offload | **PASS** | 插件已部署（autonomous preset），注册于 compaction 组首位（先裁剪→再测压），当前预设=autonomous，服务日志无报错 |

**Final Health 判定：全部关键项 PASS。**

## 3. Tag

```
rc8-reliability-v1-20260821
```

- Tag SHA：`3f4208c7c01b9bdf437de421809c098f6d778ee4`
- 与当前 origin/main 完全一致：**MATCH** ✅
- 创建状态：tag 已存在于本地且**已 push 到远端**（`ls-remote origin` 返回 `3f4208c7... refs/tags/rc8-reliability-v1-20260821`）✅
- Golden tag `rc8-golden-20260821` = `9d3884e3f2166eb80ad55255df71cdfedd462be9` 未被触碰（不可变保持）✅

> 说明：Final Health 全绿后按流程核对 tag 时发现 `rc8-reliability-v1-20260821` 已由合并流程正确创建并推送，指向合并后的 main SHA，无需也不应重复打 tag（git 拒绝重复创建，且当前状态已满足"tag 必须准确指向 origin/main"）。

## 4. Tag Push 状态

- 远端 refs/tags/rc8-reliability-v1-20260821 = `3f4208c7c01b9bdf437de421809c098f6d778ee4` ✅ **PUSHED**

## 5. Safe Boot Isolation

**PASS**

- Safe Mode 隔离验证已由 `tests/reliability/Test-FinalDrill.ps1` 全链路覆盖（isolated sandbox：DSH_BOOT_MODE_PATH / DSH_RESTART_BUDGET_PATH / DSH_SAFE_FLAG_PATH / DSH_SAFE_PROFILE_DIR / DSH_TX_ROOT 全部隔离路径）
- 本 Final Seal 流程遵守指令：**未修改 Safe Mode、未为补测试进入生产 Safe Mode、未运行生产破坏性 Chaos**
- 生产环境未被触碰（仅只读健康检查 + 诊断收集，无状态写入、无配置修改）

## 6. Real Provider-Backed Safe LLM Turn

**PARTIAL**（not destructively retested）

- 既有验证：Safe 会话的 session-core probe（Verify-SafeEnvironment 实现 + L1 会话探针）已 PASS（Stage E）
- 本次未重测真实 provider 的 Safe LLM completed turn（指令：不为此补测试、不注入故障）
- 状态如实标注为 PARTIAL，不作为 SEALED 的阻塞项

## 7. L4 Chaos

**configured, full run pending**

- `ci-level4.yml` 已配置：`workflow_dispatch` + `schedule: cron '0 3 * * 0'`（Sunday nightly）
- 内容：Reliability Lab L1 全量 + CHAOS-10 假 secret 检测（CI 必须 FAIL）
- 全量执行按设计留给 manual/nightly/release-candidate 触发，本次未运行

## 8. Final Verdict

# **SEALED**

- main SHA：`3f4208c7c01b9bdf437de421809c098f6d778ee4`（= origin/main）
- Post-merge Final Health：**14/14 关键项 PASS**（DSH 版本 / Process Identity / host.describe / session.list / events.mux / events.host / Renderer / client_ready / COMMIT_READY / Guardian / Restart Budget / Sessions / Redaction / Tool Output Offload）
- Tag：`rc8-reliability-v1-20260821` → `3f4208c7c01b9bdf437de421809c098f6d778ee4`，**已 push**
- Safe Boot Isolation：PASS
- Real provider-backed Safe LLM turn：PARTIAL（not destructively retested）
- L4 Chaos：configured, full run pending
- 未新增 commit、未修改 Safe Mode、未运行破坏性 Chaos、未为补测试注入故障

---
*Generated by Reliability v1 Final Seal — 2026-08-21T16:35+08:00*
