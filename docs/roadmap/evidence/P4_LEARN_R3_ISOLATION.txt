# P4 LEARN R3 — 会话隔离红队（最终，含根因修复）
# 生成时间: 2026-09-21 22:07:50
# 提交前最终状态：sanitizeFileId 撞名根因已消除 + loadStore 归属守卫 fail-closed


=== A. 正常会话隔离（真实 DSH 形状 sessionId） ===
  [PASS] A0 真实插件确实产出了候选（前置条件）
         A=1 B=1 logs=3
  [PASS] A1 两个会话映射到两个不同文件
         storePath(A)=session-04ecc1a4-fae9-4eb9-9a08-89b79128bcd4.json  storePath(B)=session-11112222-3333-4444-5555-666677778888.json
  [PASS] A2 磁盘上确实两个库文件
         files=[session-04ecc1a4-fae9-4eb9-9a08-89b79128bcd4.json, session-11112222-3333-4444-5555-666677778888.json]
  [PASS] A3 两库各自归属正确 sessionId
         A=session-04ecc1a4-fae9-4eb9-9a0  B=session-11112222-3333-4444-555
  [PASS] A4 A/B 候选标题无交集
         A=1 B=1 交集=0
  [PASS] A5 B 库无 A 的语料痕迹
         B=[数据库连接超时了]

=== B. sanitizeFileId 撞名（R3 根因修复验证：撞名应已消除） ===
  [PASS] B1 两个不同 sid 不再映射到同一文件（撞名已消除）
         storePath(X)=probe_session_X-4d6e3e7d.json  storePath(Y)=probe_session_X.json
  [PASS] B2 磁盘上两个独立库文件
         files=[probe_session_X-4d6e3e7d.json, probe_session_X.json]
  [PASS] B3 两库各自归属正确 sessionId（无顶替）
         X="probe session X"  Y="probe_session_X"
  [PASS] B4 无跨会话污染（后跑方不继承先跑方经验）
         Y 看到 X 语料=false; X 看到 Y 语料=false; X exps=1; Y exps=1
  [PASS] B5 守卫直测：伪造他人归属的库必须被拒绝载入并重建
         伪造后 sessionId="some-other-session" → 再驱动后 sessionId="probe session X" telemetry=[STORE_REBUILT,PROPOSED]

=== C. 超长 sessionId（>120 字符）截断撞名应已消除 ===
  [PASS] C1 超长 sid 不再撞名（两个独立文件）
         storePath 相同=false  文件数=2
  [PASS] C2 超长 sid 两库各自归属正确（无互相覆盖）
         A 归属正确=true  B 归属正确=true

=== D. 结论 ===

  PASS=13  FAIL=0
  正常 DSH sessionId 形状下隔离 = HOLDS
  撞名是否仍可达 = NO（根因已消除）
  归属守卫（fail-closed 兜底）= YES
  跨会话污染 = NOT CONFIRMED
