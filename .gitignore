*.log
probe-result.json
probe-step.log
update-check.json
vps-tunnel.log
.vs/
src/*.user
# --- secrets / credentials (2026-08-21 hardening) ---
*.pem
*.key
vps-key
secrets/
.env
.credentials.yaml
*.env.local

# --- Phase 01 (SAVE) hardening: WorkBuddy local state / runtime temp ---
.workbuddy/
_workbuddy/
*.local.json
runtime-temp/
_client-runtime/
notify-events.log
*.log.bak-*

# --- local runtime / session artifacts (never into Git) ---
client-run.log
client-run.old.log
dsh-server-*.log
*.pid
*.sock

# --- local DSH client tooling / scratch (never into repo) ---
DSH-Client/
_r51-work/

