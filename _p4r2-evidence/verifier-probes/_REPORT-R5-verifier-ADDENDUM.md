# R5 独立审查报告 · 补充判定（Addendum）

生成时间：本轮（本机）
用途：回应"后续两笔仅文档/证据提交"的补充信息，**重判第 8 点（修复后证据是否自洽）**，
并专项回答"后续提交是否掩盖了什么"。
不变式：round-1 报告 `_REPORT-R5-verifier.md`（sha256 `69E0736A9C84F46E…`）**保持原样未改**，
已随 `c1f33a2` 入库的副本与我的原件**逐字节一致**（见 §4.4）。本补充为独立文件，不覆盖该快照。

---

## 0. 首先更正两处口径（事实核对）

| 你的口径 | 实测事实 |
|---|---|
| "新 HEAD：`58bcfce7…`" | **不准确，滞后两笔**。实际 HEAD = **`c1f33a2`**；链路为 `6b73f4e → 58bcfce → 1e31537 → c1f33a2`（我复核期间又新增了 `c1f33a2`）。reflog 为**纯前进**，无 reset、无回退：`HEAD@{2}=58bcfce, HEAD@{1}=1e31537, HEAD@{0}=c1f33a2` |
| "该追加提交修复了 pristine 不自洽" | **修复者不是 `58bcfce`**。`58bcfce` **完全没有触碰** `_f1-r1-forgery-mutation-evidence.txt`（其 name-status 只有 18 个验证者探针 + 报告）。该文件在 `58bcfce` 上仍写着 `98A49D4B…`。真正的修复者是 **`1e31537`**（title 即 "rebind F1 mutation evidence to post-F2 source hash"） |

三笔后续提交**逐笔** `plugins tests` diff 全为空（我已逐笔验证）⇒ `6b73f4e` 的源码/测试确实仍是有效被评对象。
`learn.mjs = 36642349…ACAC`、`learn-core.mjs = B5BD3977…C1196` 与报告一致。

## 1. 第 8 点重判：**修复后证据自洽 → 是（且可密码学复现）**

### 1.1 原始不自洽事实（成立，我确认）
`git show <rev>:_f1-r1-forgery-mutation-evidence.txt`：

| commit | 证据文件 `date` | 证据文件记录 pristine | 同 commit 的 `learn-core.mjs` |
|---|---|---|---|
| `6b73f4e` | 00:11:25 | `98A49D4B…`（**F1 前**） | `B5BD3977…`（F2 后） ⇒ **不自洽** |
| `58bcfce` | 00:11:25 | `98A49D4B…`（**未修**） | `B5BD3977…` ⇒ **仍不自洽** |
| `1e31537` | 00:48:45 | `B5BD3977…` ✅ | `B5BD3977…` ⇒ **自洽** |

⇒ 你/前序验证者指出的瑕疵**确实存在**，且在 `58bcfce` 时**尚未修复**（比我此前的认知更严重一点：
它跨越了两笔提交）。

### 1.2 修复后是否自洽：我不用"看起来对"，而是**重跑复现**
方法：把 `_p4r2` 整体 robocopy 到临时目录（**绝不触碰仓库源文件**——该脚本会写 `plugins/learn-core.mjs`），
然后用 `_f1-r1-mutation-proof.ps1` **完全相同的变异串**与相同的 `ReadAllText/Replace/WriteAllText(UTF8 no-BOM)` 语义复跑：

| 项 | 交付证据文件记录 | 我独立复跑 | 判定 |
|---|---|---|---|
| pristine | `B5BD3977…` | `B5BD3977…` | ✅ |
| **M1 变异体 sha256** | `ED92B70881588F83B9DB31AA9828B2CAFEC29BB30C4140C6BE9A64A83B56BFBA` | **逐字节相同** | ✅ |
| M1 结果 | 19 PASS / 8 FAIL, exit 1 | 19 PASS / 8 FAIL, exit 1 | ✅ |
| M1 的 8 个 FAIL 身份 | S2.2 / S2.3 / S2.4 / S3.1 / S3.2 / S4.1 / S4.2 / S5b | **逐字、同序一致** | ✅ |
| **M2 变异体 sha256** | `0A39971FCA623A642BFAD9906EEB4FC9E124DB30F264B021F6C568F474C0B0F0` | **逐字节相同** | ✅ |
| M2 结果 | 26 PASS / 1 FAIL, exit 1 | 26 PASS / 1 FAIL, exit 1（FAIL=S7.4） | ✅ |
| 无变异 | 27 PASS / 0 FAIL, exit 0 | 27 PASS / 0 FAIL, exit 0 | ✅ |
| 恢复 | byte-identical = True | byte-identical = True | ✅ |

**关键判据（为什么这能证明"真跑过"而不是"手改哈希"）**：
变异体哈希 = f(新 pristine 内容 + 变异串)。若只是把 pristine 字段手工改成 `B5BD3977…`，
**变异体哈希必然仍是旧的** `E1B922…` / `BEB9FB…`。二者实际变成了新值 `ED92B708…` / `0A39971F…`，
且与我用新基线实测出来的值**逐字节吻合** ⇒ 只能是**在新基线上真实重跑**得到的。
变异哈希是"原像"，无法靠编辑元数据伪造。

**⇒ 第 8 点结论：修复后的证据自洽、真实、可复现。原始瑕疵事实保留在 §1.1（跨 `6b73f4e`/`58bcfce` 两笔）。**

## 2. 新发现（LOW，不阻塞）：F-R5-3 报告时间戳与证据文件不一致

报告 §6.1 L133 写：**"收尾复跑（对 F2 改动之后的当前树，2026-09-26 **00:38:28**）"**
而 `_f1-r1-forgery-mutation-evidence.txt`（`1e31537` 重写后）记录 **`date = 2026-09-26 **00:48:45**`**。

⇒ 报告引用的那次运行（00:38:28）**没有留下对应的持久化证据文件**；仓库里的证据文件是**更晚的另一次运行**。
两次的哈希与计数完全一致（我已验证后者为核心事实），故**不影响任何结论**，属**时间戳簿记瑕疵**。
- 建议：把报告该处改为 `00:48:45`，或注明"共两次复跑、数值相同、以证据文件为准"。
- 性质：与 F-R5-1 同级的文档一致性问题；**不是**证据造假信号（若是伪造，变异哈希不会对上）。

## 3. 专项："后续提交是否掩盖了什么" → **未发现任何掩盖迹象**（6 条反证）

1. **源码零改动（逐笔验证）**：`6b73f4e→58bcfce`、`58bcfce→1e31537`、`1e31537→c1f33a2`
   三笔对 `plugins tests` 的 diff **全为空**；`c1f33a2` 对 `plugins tests` 亦为空。
   没有任何"顺手改代码"。
2. **前序验证者报告以原件入库**：`_REPORT-R3-verifier.md` / `_REPORT-R4-verifier.md` 在 `58bcfce` 中
   状态为 **A（新增）而非 M（修改）**；且内容**保留了对其不利的结论**——R4 的
   `forge_full` = **VULNERABLE**、"NOT FROZEN"、"冻结前提不成立"。**未被删改、未被软化。**
   （若意在掩盖，最该动的就是这两份。）
3. **归档完整性无选择性遗漏**：前序验证者原件目录 `%TEMP%\_vfy-probes\` 中
   **没有任何文件被漏收**（"原件存在但未归档"= **空集**）；已归档 18 个与原件**逐字节一致**（differs=0）。
4. **我自己的 R5 产物未被篡改**：`c1f33a2` 收入的 18 个 R5 文件与我的原件 **18/18 逐字节一致**，
   含 `_REPORT-R5-verifier.md`（committed blob sha256 = `69E0736A9C84F46E…` = 我的原件，
   blob/worktree/原件三者 `Buffer.compare` 全等，CRLF 计数均为 0）。
5. **对我的发现转写忠实**：报告 §13 完整保留 PASS（有条件）、25/25 + 1045/0、H/I/G 组、
   **F-R5-1 明确标为 LOW 且保留"不可利用 / 与 §9.1 同一信任边界"的限定**、F-R5-2 INFO，
   无淡化、无拔高。
6. **自述残余未被删除反而被强化**：§9.1 的"宿主会话存储完整性是信任根"仍在，且新增 G1/G2
   生产链路实证（把"生产实证未做"补成"已实证"）——这是**补全**，不是**掩盖**。

⇒ **唯一发现的"补全"是正常的证据补齐**。无掩盖。

## 4. 顺带独立复核 `58bcfce`/`1e31537` 新引入的声明

| 声明 | 我的独立复核 | 判定 |
|---|---|---|
| F2 诊断常量在 `learn-core.mjs` L87-88 | 实测 L87 `export const SESSION_STORE_MAX_FILES_MIN_DIAGNOSTIC =`、L88 模板串 | ✅ |
| 三值表（63/64/65） | 与 `_f2-bounds-raw-output.txt` 逐项一致：63→`true`/effective 64/accepted `false`；64→`false`/64/`true`；65→`false`/65/`true`；三值 `anchor_present_verbatim:true` | ✅ |
| 该证据文件为 UTF-16LE | 首 4 字节 `255,254,61,0`（LE BOM） | ✅ |
| 诊断逐字原文（两条 = API 面 + `logger.warn` 面，逐字相同） | 原文中两条字符串**逐字相同**，且含逐字锚点 | ✅ |
| F2 边界套件 19 断言 | 我独立重跑 `test-learn-r2-f2-store-min.mjs` → **19 PASS / 0 FAIL, exit 0** | ✅ |
| `_final-verify-regression.txt` 全量 | 25/25 套件、**1045 PASS / 0 FAIL** ——与我独立全量运行**完全一致** | ✅ |

## 5. 诚实记录：我自己的一个测量假警报（已排除）

首轮比对时我用 PowerShell 重定向（`git show … > file`）落盘后再哈希，得到"归档报告与我原件不一致"的
信号。改用 Node `execFileSync`（buffer，无文本重定向）复核后确认：**三者逐字节相同**
（`69e0736a9c84f46e`，9453 bytes，CRLF=0）⇒ **假警报由我的测量方法引入，证据本身无问题**。
记录在此，以免该细节被误读为归档造假。

## 6. 判定更新

- **VERDICT 仍为 PASS（有条件）**，且**强于首轮**：修复后证据**自洽且可密码学复现**、**无掩盖迹象**、
  F2 新声明**全部独立成立**。
- 新增 1 项 LOW：**F-R5-3**（报告时间戳 00:38:28 vs 证据 00:48:45 不一致）——文档一致性问题，不影响判定。
- 第 8 点总结论：**原瑕疵真实存在（且跨两笔提交）；修复后自洽（已重跑复现）；修复者为 `1e31537` 而非 `58bcfce`。**

## 7. 审查者改动数

对 `_p4r2` 仓库：**0**（只读 + 在 `%TEMP%` 建探针/副本）。临时副本 `%TEMP%\_rev-p4r2\mut-repro\`
为一次性复跑工作区，可随时删除（内含被变异过的副本，**不是**仓库内容）。
