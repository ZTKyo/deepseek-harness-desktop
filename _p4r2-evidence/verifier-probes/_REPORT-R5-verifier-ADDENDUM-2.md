# R5 独立审查 · 补充判定 2（第 8 点终判：报告 ↔ 刷新后证据文件是否自洽）

生成时间：本轮（本机）
用途：回答"刷新后的证据文件是否已与报告自洽"，并给出终判与闭环状态。
不变式：`_REPORT-R5-verifier.md`（`69E0736A…`）与 `_REPORT-R5-verifier-ADDENDUM.md`（`C93F8E2C…`）
**两份均保持原样未改**，且已入库副本与我的原件**逐字节一致**。

---

## 0. HEAD 事实更正（第三次口径滞后）

| 你的口径 | 实测事实 |
|---|---|
| "HEAD 现为 `1e31537`" | **滞后两笔**。实际 HEAD = **`fd905b5`** |
| "第二个也是最后一个收口提交" | 自 `6b73f4e` 起实为 **4 笔** docs/evidence-only 提交 |

实测链路：`6b73f4e → 58bcfce → 1e31537 → c1f33a2 → fd905b5`（**纯前进**，无 reset/回退）

| commit | 说明 |
|---|---|
| `58bcfce` | F1 证据刷新 + F2 诊断契约 + 归档**前序**验证者 18 探针 |
| `1e31537` | **真正修复** pristine 不自洽（重跑重绑 + `_final-verify-regression.txt`） |
| `c1f33a2` | 归档**本轮 R5** 18 探针 + 记录 F-R5-1/F-R5-2 与 G1/G2 残余实证 |
| `fd905b5` | 修复 **F-R5-3**（报告时间戳）+ 归档**本补充报告** |

**四笔对 `plugins tests` 的 diff 全为空**；`git diff 6b73f4e HEAD -- plugins tests` **亦为空** ⇒
**被评对象 `6b73f4e` 的源码/测试始终未变**（`learn.mjs=36642349…`、`learn-core.mjs=B5BD3977…`），你的这一声明成立。

## 1. 第 8 点终判：**刷新后的证据文件已与报告自洽（8/8 字段，无残留不一致）**

| # | 字段 | 报告（L133-137 / §12） | 证据文件 `_f1-r1-forgery-mutation-evidence.txt` | 结论 |
|---|---|---|---|---|
| 1 | 复跑时间 | `2026-09-26 00:48:45` | `date = 2026-09-26 00:48:45` | ✅ **F-R5-3 已闭环**（原为 00:38:28） |
| 2 | pristine / restored / final | `B5BD3977…C1196` | `B5BD3977…C1196`（三处均同） | ✅ |
| 3 | M1 | 19 PASS / 8 FAIL，exit 1 | `exit code = 1`、`RESULT: 19 PASS / 8 FAIL` | ✅ |
| 4 | M2 | 26 PASS / 1 FAIL，exit 1 | `exit code = 1`、`RESULT: 26 PASS / 1 FAIL` | ✅ |
| 5 | 无变异基线 | 27 PASS / 0 FAIL，exit 0 | `exit code = 0`、`RESULT: 27 PASS / 0 FAIL` | ✅ |
| 6 | 恢复一致性 | `byte-identical = True` | 两处均 `byte-identical to pristine= True` | ✅ |
| 7 | 变异体哈希 | 报告**不引用**（无冲突面） | M1 `ED92B708…`、M2 `0A39971F…` —— 我已实测**逐字节复现** | ✅ |
| 8 | 代码绑定 | §12 `learn-core.mjs = B5BD3977…` | = 证据 `pristine` = 当前仓库文件（我实测一次哈希） | ✅ |

**补充说明（§1 的关键点）**：本轮此前指出的唯一残留不一致（时间戳）**已由 `fd905b5` 修复**，
且修法是最小且诚实的——把时间改为与证据文件一致的 `00:48:45`，并**保留**了另一次 `00:38:28` 复跑的说明与
"以证据文件记录为准"。这比单纯改数字更严谨：它没有抹掉那次运行的存在。

**⇒ 第 8 点终判：自洽。** 原始瑕疵（跨 `6b73f4e`/`58bcfce` 两笔的 `98A49D4B…` vs `B5BD3977…`）事实保留，
修复者 = `1e31537`，其修复我以**变异体哈希原像复现**确认（理由见 ADDENDUM §1.2）。

## 2. 是否掩盖问题 → **未发现；本轮新增 2 条反证（合计 8 条）**

7. **`fd905b5` 的改动就是那一行**：其报告 diff 仅 1 行（时间戳 + 补注），**未触碰任何结论、未删任何残余声明**。
8. **我的 `ADDENDUM` 归档逐字节一致**：入库副本 sha256 = `C93F8E2CA27CF508…` = 我的原件。

（前 6 条见 `_REPORT-R5-verifier-ADDENDUM.md` §3：逐笔源码零改动、前序验证者报告以**原件**入库且**保留对其不利的结论**（R4 `forge_full`=VULNERABLE / NOT FROZEN）、归档无选择性遗漏（"原件存在但未归档"= 空集）、我的 18 个 R5 产物未篡改、报告 §13 转写忠实、自述残余未删反被 G1/G2 补全。）

⇒ 四笔后续提交的性质均为**证据补齐/簿记修正**，**无掩盖迹象**。

## 3. 终判与闭环状态

- **VERDICT：PASS（有条件）** —— 无新增 BLOCKER / 高危；被评对象源码/测试自始至终 = `6b73f4e`。
- **第 8 点**：刷新后证据文件**已与报告自洽**（8/8 字段）；原瑕疵真实存在且跨两笔，修复者 `1e31537`，已复现验证。
- LOW 项闭环：
  - **F-R5-3（时间戳）→ 已闭环**（`fd905b5` 修复，我已复核其改动范围正确）
  - F-R5-1（`sourceEventSeqs` 契约措辞 vs 仅校验非空）→ **仍为建议**，不可利用，不阻塞
  - F-R5-2（台账链为无密钥 sha256，可重算）→ INFO，交付注释已如实说明
- 审查者改动数：**0**（`git status` 干净、源哈希未变、`6b73f4e..HEAD plugins tests` 为空）。
